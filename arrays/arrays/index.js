// array = like a variable that can stores multiple values

let fruits = ["apple", "orange", "banana"];

fruits[2] = "cocnut"; //changes the 2nd element
fruits.push("Lemon"); //adds an element to the end
fruits.unshift("mango");// adds an element to the beginning
fruits.pop(); //removes the last element
fruits.shift(); //removes elemts from beginning




let length = fruits.length;
let index = fruits.indexOf("Orange");

console.log(index);



console.log(fruits);