let fruits = ["banana", "apple", "orange", "mango"];

// fruits = fruits.sort(); // to sort by alphabeth order
fruits = fruits.sort().reverse; // reversed alphabeth order



for(let fruit of fruits)
{
    console.log(fruit);
}