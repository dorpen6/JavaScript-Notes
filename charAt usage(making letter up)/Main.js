let userName = window.prompt("Enter your username");

let letter = userName.charAt(0).toUpperCase().trim(); //we called the first letter of the username(0) by using "charAt" 

console.log(letter);
console.log(userName);